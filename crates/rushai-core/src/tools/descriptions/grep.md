Search file contents with a regular expression.

Results come back as `path:line:content`. Use `include` to restrict the
search to files matching a glob, e.g. `*.rs`. Output is capped; a trailing
note says when it was cut off.
