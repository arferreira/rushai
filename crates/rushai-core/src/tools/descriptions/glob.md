Find files by glob pattern, e.g. `src/**/*.rs`.

Matches are relative to `path` (defaults to the working directory) and come
back most recently modified first. The result list is capped; a trailing note
says when it was cut off.
